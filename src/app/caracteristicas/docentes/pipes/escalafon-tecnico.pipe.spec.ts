import { EscalafonTecnicoPipe } from './escalafon-tecnico.pipe';

describe('EscalafonTecnicoPipe', () => {
  it('create an instance', () => {
    const pipe = new EscalafonTecnicoPipe();
    expect(pipe).toBeTruthy();
  });
});
